//Java Assignment 2 - Oisin Quinn - 16314071

/*
 * We use an interface so both the Property class and the Vehicle class have the same function with the same parameters
 */

public interface Rentable {

    /**
     * This method is used to alter the number of rentalDays for an object
     * @param RentalDays the new number of rentalDays
     */
    void RentalItem(int RentalDays);
}
